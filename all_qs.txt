[ID 101] An SD-WAN zone configuration on the FortiGate GUI is shown. Based on the configuration, which statement is true? => The Underlay zone contains no member interfaces.
[ID 102] An administrator wants to configure dead peer detection (DPD) on IPsec VPN. The requirement is that FortiGate sends DPD probes only when there is no inbound traffic. Which DPD mode meets this requirement? => On Demand
[ID 103] Reviewing the diagnose debug rating output for FortiGuard connection. Which statement is true? => The weight increases as failed packets rise, and FortiGate uses the default port 8888.
[ID 104] What are two features of FortiGate FSSO agentless polling mode? => FortiGate uses the SMB protocol to read event viewer logs from DCs, and does not support workstation check.
[ID 105] An administrator wants to form an HA cluster using the FGCP protocol. Which two requirements must both members fulfill? => They must have the same HA group ID, and the same hard drive configuration.
[ID 106] Troubleshooting an IPsec tunnel: phase 1 is up, but phase 2 fails. HQ-NGFW shows Local: 10.0.11.0/24, Remote: 172.20.1.0/24, AES128. BR1-FGT shows Local: 172.20.1.0/24, Remote: 10.11.0.0/24, AES256. => On BR1-FGT, set Remote Address to 10.0.11.0/255.255.255.0, and on HQ-NGFW, set Encryption to AES256.
[ID 107] In an SSL/SSH Inspection Profile, 'Server certificate SNI check' is set to 'Strict'. What is the impact? => FortiGate will close the connection if the SNI does not match the CN or SAN fields.
[ID 108] Application Overrides: Priority 1 is Excessive-Bandwidth (Block). Priority 2 is Google (Monitor). Inspection is Proxy-based. Result: Google apps are blocked, while fortinet.com works. How to resolve? => Move up Google in the Overrides section to set its priority higher, and set its action to Allow.
[ID 109] With 'set fail-open enable' configured in IPS global, what happens when FortiGate enters conserve mode? => FortiGate refuses to accept configuration changes, and continues to transmit packets without IPS inspection.
[ID 110] A dialup IPsec VPN is configured with add-route enabled, but the static route is not showing in the routing table. Why? => Phase 2 must be successfully established, and the remote network must be defined correctly in phase 2 selectors.
[ID 111] What is the impact of enabling 'Include in every user group' in a RADIUS server configuration? => It places the RADIUS server, and all users who can authenticate against that server, into every FortiGate user group.
[ID 112] A web filter profile with a daily category usage quota is not listed in the available web profile drop-down in a firewall policy. Reason? => The inspection mode in the firewall policy is not matching with the web filter profile feature set (e.g., flow-based policy vs proxy-based quota).
[ID 113] A One-to-One Dynamic IP Pool has 2 External IPs (100.65.0.110-100.65.0.111). PC1 and PC2 connect fine, but PC3 fails to reach the internet. Solution? => In the IP pool, set end ip to 100.65.0.112 (adding a third IP), or set type to overload (PAT).
[ID 114] HA status: HQ-NGFW-1 is Primary (Priority 90, Override disabled). HQ-NGFW-2 is Secondary (Priority 110, Override enabled). What happens if they were just reconfigured this way? => HQ-NGFW-2 will take over as the primary because it has override enabled and higher priority.
[ID 115] Active authentication (captive portal) is configured for a policy allowing HTTP and HTTPS. Users don't get a login prompt. Why? => The Service DNS is required in the firewall policy.
[ID 116] Which of the following is true regarding firewall policy IDs? => A policy ID cannot be modified once created, and you can create a policy in CLI with policy ID 0 (auto-assign).
[ID 117] Ping traffic from HQ-PC-1 matches a policy named 'PING traffic' utilizing the SNAT-Remote1 IP pool (100.65.0.99). Which SNAT IP will be applied? => 100.65.0.99
[ID 118] How does NTurbo acceleration enhance antivirus performance on supported FortiGate hardware? => For flow-based inspection, NTurbo establishes a dedicated data path to redirect traffic between the IPS engine and FortiGate ingress/egress interfaces.
[ID 119] You created an IPS profile, but 'diagnose test application ipsmonitor 1' shows engine count = 0. What is the reason? => There is no firewall policy configured with an IPS security profile.
[ID 120] You filter FortiGate UTM Application Control logs. Which two methods can correctly locate these logs? => In the Forward Traffic log section, and by filtering by policy UUID/application name.
[ID 141] FortiGate is operating in NAT mode with interfaces connected to LAN and DMZ. Which two statements about the requirements for these physical interfaces are true? => Both interfaces must have IP addresses assigned, and must have directly connected routes on the routing table.
[ID 142] A firewall policy is configured with Proxy-based inspection, but a Web Filter profile with 'Feature set: Flow-based' is applied. Traffic is being blocked incorrectly. What is the cause? => The web filter profile feature set (Flow-based) mismatches the firewall policy inspection mode (Proxy-based), causing unexpected behavior.
[ID 143] To prevent GUI sessions from disconnecting too early for the NOC team, which uses the 'NOC_Access' admin profile, what must be configured? => Increase the Override Idle Timeout parameter within the NOC_Access admin profile.
[ID 144] In an HA cluster, what triggers a link failover when a monitored interface experiences an issue? => Link failover is triggered if the monitored interface goes down, effectively reducing the unit's priority.
[ID 145] Which two statements describe characteristics of automation stitches? => Multiple actions can run in parallel, and triggers can involve external connectors.
[ID 146] Which of the following are valid SD-WAN rule strategies for member selection? => Lowest Cost (SLA) without load balancing, Manual with load balancing, and Lowest Cost (SLA) with load balancing.
[ID 147] Which statements explain a flow-based antivirus profile using the hybrid scanning mode in FortiOS? => FortiGate buffers the whole file but transmits to the client at the same time, optimizing performance compared to proxy-based inspection.
[ID 148] A downstream FortiGate (HQ-ISFW-2) is added to the Security Fabric on the same subnet as the root. Its status remains 'Pending'. Why? => The upstream FortiGate IP must point to the root's listening interface, and the downstream device must be manually authorized on the root.
[ID 149] What is the primary FortiGate election process when the HA 'override' setting is ENABLED? => Connected monitored ports > Priority > HA uptime > FortiGate serial number.
[ID 150] A firewall policy with an Antivirus profile allows HTTPS traffic, but EICAR malware over HTTPS is not blocked. What is the most likely cause? => The SSL inspection mode applied to the policy is 'certificate-inspection' instead of deep content inspection.
[ID 151] Both HA members have 'override' DISABLED. HQ-NGFW-1 has Priority 200, HQ-NGFW-2 has Priority 100. After a week, how can an administrator manually force a clean failover from HQ-NGFW-1 to HQ-NGFW-2? => The administrator must run 'diagnose sys ha reset-uptime' on HQ-NGFW-1.
[ID 152] What are two key routing principles regarding SD-WAN rules and the Forwarding Information Base (FIB)? => By default, SD-WAN rules are skipped if the resolved FIB best match isn't an SD-WAN member, and regular policy routes take precedence over SD-WAN rules.
[ID 153] When configuring Equal-Cost Multi-Path (ECMP), how does SD-WAN status affect the load balancing algorithm config? => If SD-WAN is enabled, you control the algorithm via 'load-balance-mode' under config system sdwan. If disabled, it's done via 'v4-ecmp-mode' in global settings.
[ID 154] An address object created on the root FortiGate with 'Fabric global object' enabled is NOT visible on downstream members. The root FortiGate has 'fabric-object-unification' set to 'local'. Fix? => Change the csf setting on the root FortiGate to set fabric-object-unification to 'default'.
[ID 155] An administrator wants to address Shadow IT visibility and prevent users from sending sensitive files to unsanctioned cloud apps. Which FortiSASE method fits best? => Secure SaaS access (SSA)
[ID 156] To block traffic when an IPS signature is triggered a specific number of times within a defined duration, how should the IPS sensor be configured? => Use IPS signatures, and set the rate-mode to the 'periodical' option.
[ID 157] When configuring a FortiAnalyzer connection, which GUI element confirms that reliable, encrypted transport (TCP/OFTP) is actively enabled for logging? => A padlock icon appears in the connection settings.
[ID 158] Why would an administrator enable 'session preservation' on a WAN interface in a multi-WAN arrangement? => To ensure that existing FortiGate-terminated sessions (like SSL VPN) remain on the same interface even if routing changes occur.
[ID 159] When onboarding an agentless Secure Web Gateway (SWG) endpoint using a PAC file for FortiSASE, what happens to the user's non-web traffic (e.g., SSH, FTP)? => All non-web traffic will bypass FortiSASE and be forwarded directly to the internet.
[ID 160] A diagnostic debug flow command outputs: 'policy-0 is matched, act-drop' and 'Denied by forward policy check (policy 0)'. Why did FortiGate drop the packet? => It matched the default implicit deny firewall policy (policy 0).
[ID 121] SD-WAN rules are configured, but traffic logs do not show the name of the SD-WAN rule used to steer traffic. Why? => FortiGate load balanced the traffic according to the implicit SD-WAN rule.
[ID 122] Why do firewall policies appear in a different order when switching between Interface Pair View and By Sequence View? => Interface Pair View sorts policies based on matching interfaces, while By Sequence View shows the actual processing order of rules.
[ID 123] Which step is NOT part of the expected process when an administrator configures FSSO using DC Agent Mode? => The DC agent sends login event data directly to FortiGate.
[ID 124] Which statement correctly describes the NetAPI polling mode for the FSSO collector agent? => The NetSessionEnum function is used to track user logouts.
[ID 125] An HA cluster has HQ-NGFW-1 memory at 90% and HQ-NGFW-2 at 48%. The memory-failover-threshold is 70 and monitor period is 50s. After 55 seconds, which unit is primary? => HQ-NGFW-2 becomes primary due to the memory-failover-threshold setting.
[ID 126] When deploying a FortiGate Cloud-Native Firewall (CNF) in AWS, which component must be created to handle traffic from an EC2 instance? => The gateway load balancer endpoint (GWLBe) in the customer virtual private cloud (VPC).
[ID 127] An Application Override is configured to 'Allow' ABC.Com. Traffic is allowed, but no security logs are generated for ABC.Com despite logging enabled on the firewall policy. Why? => The ABC.Com Action is set to Allow. Allowed traffic generates traffic logs, but not application control security logs.
[ID 128] Memory usage is at 90%. Conserve mode thresholds: green 82%, red 88%, extreme 89%. What happens? => FortiGate has entered conserve mode. New sessions are dropped. Administrators can still change configurations.
[ID 129] Which two components are part of the secure internet access (SIA) agent-based mode on FortiSASE? => FortiSASE Firewall-as-a-Service (FWaaS) and VPN policies.
[ID 130] Why do predefined deep-inspection profiles exclude some web categories (like Finance and Banking) from SSL inspection? => Legal regulations prioritize user privacy, and temporary certificates break websites using HTTP Strict Transport Security (HSTS).
[ID 131] When creating a new antivirus profile for FTP, the Antivirus scan switch is grayed out. What is the cause? => None of the inspected protocols are active in this profile.
[ID 132] When creating a firewall policy, which attribute must an administrator include to enhance functionality and enable log correlation on FortiAnalyzer and FortiManager? => Universally Unique Identifier (UUID)
[ID 133] How does FortiExtender connect to FortiSASE in a site-based, remote internet access method? => It uses a Virtual Extensible LAN (VXLAN)-over-IPsec connection.
[ID 134] What are two features of the FSSO collector agent advanced mode? => FortiGate can be configured as an LDAP client to apply group filters, and it supports nested or inherited groups.
[ID 135] Global config defines 'set strict-src-check enable'. Interface port1 defines 'set src-check disable'. What is the impact? => FortiGate will enable strict RPF on all its interfaces, but port1 will be exempted from RPF checks.
[ID 136] You want a new static route to subnet 172.20.1.0/24 to route through port2. An existing static route via port3 has distance 9. What two criteria achieve this? => Set the new port2 route distance to 9, and increase the existing port3 route distance to 11.
[ID 137] There are multiple dialup IPsec VPNs configured in aggressive mode on FortiGate. Which Phase 1 setting matches the specific user to their respective tunnel? => Peer ID
[ID 138] With full SSL inspection enabled, visiting HTTPS websites throws browser certificate warning errors. What is the reason? => The browser does not trust the certificate authority (CA) used by FortiGate for SSL inspection.
[ID 139] Peer-to-peer traffic is set to Block under the Categories tab of an Application Control profile, yet P2P traffic on known ports still passes. What setting should you check? => Application and Filter Overrides
[ID 140] What are two characteristics of HA cluster heartbeat IP addresses? => They are used to distinguish between cluster members, and they dynamically change when a device joins or leaves the cluster.
[ID 161] A remote authentication server is configured to return the vendor-specific attribute 'Fortinet-Group-Name' with a value of 'Training'. Why does the FortiGate administrator need this configuration? => To authenticate and match the authenticated user to the corresponding 'Training' FortiGate user group.
[ID 162] Which three methods are used by the collector agent for AD polling? => NetAPI, WMI, and WinSecLog
[ID 163] An administrator has created a new FQDN firewall address object (www.fortinet.com) to use as the destination for a static route, but cannot select it in the Destination field of the new static route. Why? => In the new firewall address, Routing configuration ('allow-routing') must be enabled.
[ID 164] An administrator configures FortiGuard servers as DNS servers on FortiGate using default settings. What is true about the DNS connection to a FortiGuard server? => It uses DNS over TLS (DoT) by default.
[ID 165] An IPS sensor profile is configured to add the 'FTP.Login.Failed' signature with 'Packet logging' enabled. What can you conclude from this setup? => FortiGate stores a local copy of the packet that matches the signature.
[ID 166] An administrator must block access to 'download.com', which belongs to the 'Freeware and Software Downloads' category, while still allowing other websites in that same category. What are two solutions? => Configure a static URL filter entry for download.com with Action Block, or configure a web override rating for download.com to a blocked category.
[ID 167] You have configured FortiGate for FSSO. A user successfully logs into Windows, but their access to the internet is denied by the internet-access policy. What should the administrator check first? => The FortiGate FSSO active users list for the user's IP address.
[ID 168] A system log states: 'msg="IPS session scan, enter fail open mode"' and 'action="drop"'. What does this indicate? => The IPS socket buffer is full and the IPS engine lacks memory/resources to create new sessions, so it drops them based on the fail-open setting.
[ID 169] An administrator configures 'set ses-denied-traffic enable' under system settings and sets 'block-session-timer' to 30. What are the results? => A session for denied traffic is created in the session table, and the number of logs generated for repeated denied traffic is reduced.
[ID 170] FortiGate has two identical firewall policies mapping to the same web server, except one applies to 'port1' (Sales) and the other to 'port2' (Engineering). How can the administrator consolidate them into one policy via the GUI? => Enable 'Multiple Interface Policies' on the Feature Visibility page to allow selecting both port1 and port2 as incoming interfaces.
[ID 171] An administrator wants to address shadow IT visibility challenges and prevent users from sending sensitive files outside the organization without proper approval. Which FortiSASE method should the administrator implement? => Secure SaaS access (SSA)
[ID 172] An administrator wanted to configure an IPS sensor to block traffic that triggers the signature set number of times during a specific time period. How can the administrator achieve the objective? => Use IPS signatures, rate-mode periodical option.
[ID 173] When configuring the connection between FortiGate and FortiAnalyzer, which option indicates that reliable traffic is enabled? => A padlock icon appears in the connection settings.
[ID 174] You have configured the FortiGate device for FSSO. A user is successful in logging into Windows, but their access to the internet is denied. What should the administrator check first? => The FortiGate FSSO active users list for the user's IP address.
[ID 175] When configuring a FortiGate in a multi-WAN setup, why would an administrator enable session preservation on an interface? => To ensure that existing SSL VPN connections remain on the same interface even if route changes occur.
[ID 176] An antivirus profile is configured for FTP, HTTP, and HTTPS. Blocking works for FTP and HTTP, but FortiGate does not block an infected file downloaded over HTTPS. What is the cause? => The SSL inspection mode in the firewall policy is not deep content inspection.
[ID 177] When downloading an EICAR test file through HTTP, FortiGate detects it. Through HTTPS, it does not. What are two possible reasons? => The SSL inspection profile has certificate inspection enabled, or the website is exempted from SSL inspection.
[ID 178] You are onboarding an agentless, secure web gateway (SWG) endpoint for secure internet access (SIA). What will happen to the user's nonweb traffic? => All the nonweb traffic will bypass FortiSASE.
[ID 179] An administrator has created a new firewall address to use as the destination for a static route, but the address is not appearing in the Destination field dropdown. Why? => In the new firewall address, Routing configuration must be enabled.
[ID 180] When FortiGuard servers are configured as DNS servers on FortiGate using default settings, what protocol and port are used? => It uses DNS over TLS (DoT).
[ID 181] An IPS Sensor profile is configured to block the 'FTP.Login.Failed' signature and has 'Packet logging' enabled. What happens? => FortiGate stores a local copy of the packet that matches the signature.
[ID 182] You set peer-to-peer traffic to Block under the Categories tab, but P2P traffic is still passing through unblocked. What should you check? => Application and Filter Overrides.
[ID 183] Sales (port1) and Engineering (port2) have identical firewall policies going to the same web server. How do you consolidate them into one policy? => Enable Multiple Interface Policies to select port1 and port2 in the same firewall policy.
[ID 184] System performance shows 90% memory usage. Default thresholds are green 82%, red 88%, extreme 89%. What are the two outcomes? => FortiGate has entered conserve mode, and administrators can still change configuration.
[ID 185] Log states: logdesc="IPS session scan paused", msg="IPS session scan, enter fail open mode", action="drop". What does this conclude? => The IPS socket buffer is full and IPS engine needs more memory to create new sessions.
[ID 186] A debug flow output shows: "policy-0 is matched, act-drop" and "Denied by forward policy check (policy 0)". Why did FortiGate drop the packet? => It matched the default implicit firewall policy.
[ID 187] An administrator configures 'set ses-denied-traffic enable' and 'set block-session-timer 30'. What are the results of this configuration? => The number of logs generated by denied traffic is reduced, and a session for denied traffic is created.
